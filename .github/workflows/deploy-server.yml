name: Deploy Node Server to EC2

on:
  push:
    branches:
      - main
    paths:
      - 'server/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploying to EC2 via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_PRIVATE_KEY }}
          script: |
            # Navigate to the root of the repo to execute Git commands
            cd /home/ubuntu/ISA-WEB
            
            # Pull the latest code (Sparse-checkout will only grab the server folder updates)
            git pull origin main
            
            # Go into the server folder to install packages
            cd server
            npm install
            
            # Restart the server
            pm2 restart api_server